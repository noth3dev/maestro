# Pending live acceptance checks

- [ ] E4 channel/sidebar Goal-context replay — **partial evidence recorded**: real authenticated tmux captures at 80x24, 120x40, and 200x50 with color and `NO_COLOR` now show the selected Goal and a real Goal-bound channel list/page without `No Goal is selected` (`.artifacts/e4-page-layout-post-final/`, `.artifacts/e4-nav-page-audit-20260921/`). Remaining: activate/read individual `#general` and `#head-council` selectors in a live session.

- [ ] plan-3 § S4 — first-usable-release fourteen-step real-provider gate — 실행 방법: `cat test/release-scenario/RUNBOOK.md` and execute all fourteen steps in one clean scenario
- [ ] plan-4 § S4 — narrow-grant and incident-under-outage live scenarios — 실행 방법: `cat test/phase4-scenario/RUNBOOK.md` and execute Scenario A and Scenario B using `testbed/README.md` projects #1–#4
- [ ] plan-5 §5 HANDOFF — three-goal two-project concurrent portfolio live acceptance (capacity contention, Portfolio Council decision, safe pause/resume, CEO pin, Discord safety preemption) — 실행 방법: `cat test/phase5-scenario/RUNBOOK.md` and execute the eight-step live walkthrough in one clean scenario
- [ ] E4 current-view marker live natural-language/development recheck remains blocked before model execution: real authenticated tmux home captures completed, but no model response or Task Contract/Head/Worker/Metronome/Encore/release/provider acceptance was attempted in this render-only slice. Preserve the separate full orchestration gate and do not claim it from UI captures. Evidence: `.artifacts/e4-current-view-narrow-matrix/` and the existing E4 release runbook items.
- [ ] E4 live natural-language and development delegation — latest safe read-only prompt reached the real TUI but stopped at `Input error: Requested model is not allowed` after the visible model label was shown. No model response, Task Contract, approval, Head/Council, DepartmentPlan, MissionBundle, Worker, Evidence, Metronome, Encore, release, or provider acceptance is claimed. Retry only when the provider model allowlist/auth boundary is available. Evidence: `.artifacts/e4-live-natural-language-retry-20260921.txt`.
- [ ] plan-E5 — live Electron/Postgres/provider verification remains blocked as of 2026-09-21. Reproduction: `echo "$MAESTRO_TEST_DATABASE_URL"` returns empty; `docker ps` shows only unrelated Supabase containers; `ss -ltnp | grep -E ":(3000|4000|9222|55432|5432)"` shows only Supabase ports, with no Control Plane/provider listener. An Electron process exists, but no E5 acceptance path was exercised. All matrix entries remain static-code-read/unit-test only; run Task 13/14's live checks when a usable database, Control Plane, and provider are configured. 실행 방법: `cd apps/carnegie && npm run build && npx playwright test --reporter=line`, then `npm run --workspace @maestro/carnegie start -- --remote-debugging-port=9222` per plan-E5 Task 14 Step 1.
- [ ] E4 sidebar channel-row destination replay — pre-fix live evidence is preserved in `.artifacts/e4-channel-selector-audit-20260921/`: selecting the loaded `#metronome` row used transcript/composer instead of the destination-page main pane, while `#general`/`#head-council` were absent from that roster snapshot. The approved minimal `openReadView` implementation is merged as `a57f5c54`, but no post-fix live acceptance is claimed: run#48 and the run#52 post-merge 80x24/120x40/200x50 color/`NO_COLOR` matrix all restored `Session attached` but showed Control Plane authentication failure `Durable store is unavailable`; `ctrl+r` reproduced the blocker. Evidence: `.artifacts/e4-channel-row-post-merge-run52/`. Re-run individual channel reads after the session/database blocker is cleared.
- [ ] plan-E5 Task 3 Step 8 — first real Electron Concertmaster conversation attempted on 2026-09-21 but not completed. Evidence: `MAESTRO_TEST_DATABASE_URL` was empty; `docker ps` exposed only Supabase services (`54322`, `54321`, `54324`), and no Control Plane/provider listener was present on `3000`, `4000`, `9222`, `55432`, or `5432`. The prescribed `npm run --workspace @maestro/carnegie start -- --remote-debugging-port=9222` reached `DevTools listening on ws://127.0.0.1:9222/...`, then logged `[renderer] Uncaught (in promise) Error: Control plane request failed`; no project, conversation ID, contract ID, or real assistant response was available to capture. Display/Wayland were present, so the blocker is backend/provider availability, not display setup. Re-run the bounded non-destructive Electron conversation after a usable Control Plane, database, and provider are configured. **Root cause named 2026-09-21: this and the entry above are both symptoms of plan-E5 Task 1.5 (zero-config local bootstrap) not existing yet — `packages/local-backend/src/local-bootstrap.ts`'s `resolveLocalConnection` is a complete, tested local-stack launcher that `apps/carnegie/electron/main.ts` never calls. Build Task 1.5 first, then re-attempt this.**
