# Pending live acceptance checks

- [ ] plan-3 § S4 — first-usable-release fourteen-step real-provider gate — 실행 방법: `cat test/release-scenario/RUNBOOK.md` and execute all fourteen steps in one clean scenario
- [ ] plan-4 § S4 — narrow-grant and incident-under-outage live scenarios — 실행 방법: `cat test/phase4-scenario/RUNBOOK.md` and execute Scenario A and Scenario B using `testbed/README.md` projects #1–#4
- [ ] plan-5 §5 HANDOFF — three-goal two-project concurrent portfolio live acceptance (capacity contention, Portfolio Council decision, safe pause/resume, CEO pin, Discord safety preemption) — 실행 방법: `cat test/phase5-scenario/RUNBOOK.md` and execute the eight-step live walkthrough in one clean scenario
- [ ] E4 environment gate — `npm run build` reaches root/CLI `tsc -b`, but Carnegie renderer typecheck is unavailable in this worktree because `apps/carnegie` imports `@playwright/test` 1.62.1 without an installed module/types; current output is captured in `.artifacts/e4-build-current.log`; rerun the full build after restoring workspace dependencies.
- [x] E4 live capture hardening — current live capture uses a bounded `Session attached` readiness predicate, observable command submit/settle, and explicit one-pass mode for dynamic matrices. Six live `/help` cases are in `.artifacts/e4-pillar1-live-help-final3` with `reproducible: false`; the independent blind review is PASS. PageUp/PageDown reachability is in `.artifacts/e4-pillar3-live-help-pageup-verify`.
- [x] E4 command catalog readability — live `/help` was captured at 80x24, 120x40, and 200x50 in color and `NO_COLOR` with dynamic results marked `reproducible: false`. The catalog is grouped by safety class, local commands are compressed, and READ/WRITE/CRITICAL representatives remain visible in the 80x24 tail. Fresh blind review: PASS. Evidence: `.artifacts/e4-postcommit-live-help` and `.artifacts/e4-postcommit-live-help-matrix`; pre-commit captures are superseded.
- [x] E4 natural-language conversation — a real logged-in TUI accepted a read-only prompt and returned `LIVE-READONLY-OK`; no goal creation, tool call, file write, external action, approval, publish, logout, or credential mutation was requested. Evidence: `.artifacts/e4-live-conversation.txt`.
- [x] E4 read-only live views — real 120x40 frames verified `/events list`, `/channel list`, `/billing get`, and `/models list`; `/goal get --goal-id ad813d92-38d4-401d-b957-14d49c813932` returned the draft Goal. `/goal list` was rejected because the command requires an action; this is recorded as an invalid probe, not a product failure. Evidence: `.artifacts/e4-live-readonly/output.txt` and `.artifacts/e4-live-goal-get.txt`.
- [ ] E4 broader release runbooks — this slice does not claim completion of Goal creation, approval, Council/DepartmentPlan/Worker execution, publish, or the plan-3/4/5 release scenarios.
- [x] E4 narrow sidebar overlay — committed in `610c4d75` plus footer cue in `9a247974`: modal-visible keyboard input is excluded from sidebar focus, narrow `Ctrl+B` opens a full-width navigation overlay with `esc unfocus · ctrl+b` recovery, and normal-width/composer restoration shows the `ctrl+b sidebar` footer cue. Evidence: `.artifacts/e4-pillar1-live-sidebar-overlay` and `.artifacts/e4-pillar1-live-sidebar-overlay-enter`; the earlier `.artifacts/e4-narrow-sidebar-live` overlay frames are superseded. Fresh blind review: PASS.
- [ ] E4 authenticated color/NO_COLOR parity — latest live matrix is six dynamic cases marked `reproducible: false`; paired 200x50 rerun showed non-equivalent transient auth/history state between color and `NO_COLOR`. Source has no `NO_COLOR` auth branch. Do not claim live backend parity until same-session controlled evidence exists. Evidence: `.artifacts/e4-latest-head-live-matrix` and `.artifacts/e4-live-color-pair-rerun`.
- [x] E4 channel empty-state clarity — real sidebar channel Enter flow on the attached draft Goal now distinguishes Goal-scoped channels from organization sidebar channels. Evidence: `.artifacts/e4-live-channel-enter/color-after-escape-v3.txt`; fresh blind review: PASS.
