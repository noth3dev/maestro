# Pending live acceptance checks

- [ ] **Integrated E5 Overture gate (2026-09-23):** the former E6 plan is now folded into `execution/plan-E5-gui-implementation.md` Tasks 14–20. The authoritative path is Concertmaster → same-channel Overture Crew → `plan00.md` → phase plans → multiple slice plans → hash-bound Task Contract → exact Launch → automatic Goal orchestration. The old E6 file is an archival pointer and must not be executed separately. Live acceptance remains open until the real Overture Run, plan manifest, Task Contract, and post-launch IDs/evidence are observed.

- [ ] plan-E8 — native ChatGPT Codex OAuth + Responses API provider (`.worktrees/e8-codex-responses`, not yet merged) is code-complete, independently reviewed, and passes 311/312 files / 2147/2156 tests non-integration, but has **no live acceptance**: no real browser OAuth consent has been completed against this new credential path, and no real tool-bearing turn has been observed end-to-end. This is a genuinely separate credential store from the legacy app-server path's `~/.codex/auth.json` (used by plan-E7's live check), so it needs its own fresh login. 실행 방법: build the worktree (`npm run build` from `.worktrees/e8-codex-responses`), launch `apps/model-gateway` + `apps/control-plane` WITHOUT `MAESTRO_CODEX_APP_SERVER_COMMAND` set (native path is default), run `maestro login openai-codex` (operator completes the real browser consent), then drive one goal-less Concertmaster turn through Carnegie (using plan-E7's "start new conversation" fix) or via a direct `/v1/conversations/.../turns` call, and confirm a real assistant response with the `task-contract:create` tool actually invoked (not `Model turn outcome is unavailable`). See `roadmap/act-1-foundation/active/operations/findings.md`'s 2026-09-22 plan-E8 entry for full implementation detail.

- [ ] E4 channel/sidebar Goal-context replay — **partial evidence recorded**: real authenticated tmux captures at 80x24, 120x40, and 200x50 with color and `NO_COLOR` now show the selected Goal and a real Goal-bound channel list/page without `No Goal is selected` (`.artifacts/e4-page-layout-post-final/`, `.artifacts/e4-nav-page-audit-20260921/`). Remaining: activate/read individual `#general` and `#head-council` selectors in a live session.

- [ ] plan-3 § S4 — first-usable-release fourteen-step real-provider gate — 실행 방법: `cat test/release-scenario/RUNBOOK.md` and execute all fourteen steps in one clean scenario
- [ ] plan-4 § S4 — narrow-grant and incident-under-outage live scenarios — 실행 방법: `cat test/phase4-scenario/RUNBOOK.md` and execute Scenario A and Scenario B using `testbed/README.md` projects #1–#4
- [ ] plan-5 §5 HANDOFF — three-goal two-project concurrent portfolio live acceptance (capacity contention, Portfolio Council decision, safe pause/resume, CEO pin, Discord safety preemption) — 실행 방법: `cat test/phase5-scenario/RUNBOOK.md` and execute the eight-step live walkthrough in one clean scenario
- [ ] E4 current-view marker live natural-language/development recheck remains blocked before model execution: real authenticated tmux home captures completed, but no model response or Task Contract/Head/Worker/Metronome/Encore/release/provider acceptance was attempted in this render-only slice. Preserve the separate full orchestration gate and do not claim it from UI captures. Evidence: `.artifacts/e4-current-view-narrow-matrix/` and the existing E4 release runbook items.
- [ ] E4 live natural-language and development delegation — latest safe read-only prompt reached the real TUI but stopped at `Input error: Requested model is not allowed` after the visible model label was shown. No model response, Task Contract, approval, Head/Council, DepartmentPlan, MissionBundle, Worker, Evidence, Metronome, Encore, release, or provider acceptance is claimed. Retry only when the provider model allowlist/auth boundary is available. Evidence: `.artifacts/e4-live-natural-language-retry-20260921.txt`.
- [ ] plan-E5 — live Electron/provider verification remains blocked. The 2026-09-22 database note below is historical, not current environment proof. The current Task 14 attempt reached the real Electron/CDP window but stopped at existing PGlite bootstrap; no Control Plane/provider listener or durable flow was available. See the dated Task 14/15 entries below and `execution/e5-live-evidence/README.md`.
- [x] **Historical environment note (2026-09-22; not current proof):** a disposable Maestro Postgres is now running — `docker run -d --name maestro-local-postgres --restart unless-stopped -e POSTGRES_USER=maestro -e POSTGRES_DB=maestro_local -e POSTGRES_HOST_AUTH_METHOD=trust -p 127.0.0.1:55432:5432 -v maestro-local-postgres-data:/var/lib/postgresql/data postgres:17-alpine` (the exact image/container name/port `local-bootstrap.ts`'s own Docker path already uses, so this doubles as a live exercise of that code path). It has `--restart unless-stopped`, so it survives reboots of this host. **If `$MAESTRO_TEST_DATABASE_URL` is empty in your shell**, the export was added to `~/.bashrc` but may not have propagated to your session (interactive-shell-only sourcing) — just export it yourself before running integration tests: `export MAESTRO_TEST_DATABASE_URL="postgresql://maestro@127.0.0.1:55432/maestro_local"`. Verified: `packages/persistence/src/actual-cost.integration.test.ts` passes 6/6 against this instance. A full unrestricted `npx vitest run` (integration + property suites included, no exclusions) ran 2026-09-22: **403/404 files, 2682/2683 tests passed.**
- [x] **SSE acknowledgement regression resolved 2026-09-22:** `apps/control-plane/src/tui-sse-reconnect.integration.test.ts` now asserts the production `connected` acknowledgement before the durable event, preserving the bridge contract introduced by `426c9bfb`. Focused PostgreSQL-backed verification passes 2/2 in `apps/control-plane/src/tui-sse-reconnect.integration.test.ts`.
- [ ] E4 sidebar channel-row destination replay — pre-fix live evidence is preserved in `.artifacts/e4-channel-selector-audit-20260921/`: selecting the loaded `#metronome` row used transcript/composer instead of the destination-page main pane, while `#general`/`#head-council` were absent from that roster snapshot. The approved minimal `openReadView` implementation is merged as `a57f5c54`, but no post-fix live acceptance is claimed: run#48 and the run#52 post-merge 80x24/120x40/200x50 color/`NO_COLOR` matrix all restored `Session attached` but showed Control Plane authentication failure `Durable store is unavailable`; `ctrl+r` reproduced the blocker. Evidence: `.artifacts/e4-channel-row-post-merge-run52/`. Re-run individual channel reads after the session/database blocker is cleared.
- [ ] plan-E5 Task 3 Step 8 — first real Electron Concertmaster conversation attempted on 2026-09-21 but not completed. Evidence: `MAESTRO_TEST_DATABASE_URL` was empty; `docker ps` exposed only Supabase services (`54322`, `54321`, `54324`), and no Control Plane/provider listener was present on `3000`, `4000`, `9222`, `55432`, or `5432`. The prescribed `npm run --workspace @maestro/carnegie start -- --remote-debugging-port=9222` reached `DevTools listening on ws://127.0.0.1:9222/...`, then logged `[renderer] Uncaught (in promise) Error: Control plane request failed`; no project, conversation ID, contract ID, or real assistant response was available to capture. Display/Wayland were present, so the blocker is backend/provider availability, not display setup. Re-run the bounded non-destructive Electron conversation after a usable Control Plane, database, and provider are configured. **Root cause updated 2026-09-21:** the Task 1.5 Carnegie startup wiring is now implemented and verified at the code/build/test level: `apps/carnegie/electron/main.ts` reaches `initializeCarnegieConnection`, forwards real bootstrap progress, and the renderer exposes a setup-required fallback. Live first-run acceptance remains pending because this sandbox still has no usable Control Plane/provider listener or packaged local binaries. Re-run the bounded non-destructive Electron conversation after a live bootstrap-capable environment is available.**
- [ ] plan-E5 Task 6 — Worker-originated channel conversation projection is not yet wired end to end. Code evidence: `packages/persistence/src/channel.ts` supports bounded internal `head`/`worker` authors only when a `channel-runtime` proof is supplied, while `apps/control-plane/src/worker-service.ts` `sendMessage` calls `sendWorkerMessageUnderOwnerClaim` and does not append a Worker-authored Channel message; the public API client exposes only `sendWorkerMessage` and cannot supply an internal author proof. The Carnegie slice therefore renders only durable Channel messages and Worker observations/tool events and must not simulate agent-to-agent messages. Owning layer: Control Plane/persistence worker-to-channel projection. Smallest next check: define and integration-test the durable event/channel write contract for bounded Goal-scoped Worker collaboration before exposing Worker-authored messages in Carnegie. Evidence: `packages/persistence/src/channel.ts`, `apps/control-plane/src/worker-service.ts`, `packages/api-client/src/methods/workers.ts`.


- [ ] **plan-E5 Task 14 live dogfood — attempted 2026-09-23, backend-blocked:** Carnegie build and real Electron/CDP startup passed at `e6dc7db8`. The renderer reached the genuine recovery screen, but `bootstrap.status()` was `setup-required` because the existing embedded PGlite data timed out on its mutex. No Control Plane (`4310`), Model Gateway (`4321`), or embedded database (`55433`) listener appeared. Fresh disposable PGlite data passed; a copied existing-data recovery failed with `RuntimeError: Aborted()`. The original database and credentials were not deleted/reset/rotated. Playwright against the real CDP target reached the renderer but could not proceed past recovery. No IDs, provider response, Goal/Worker/evidence/certification/report records, or external effects exist. Evidence: `execution/e5-live-evidence/README.md`.

- [ ] **plan-E5 Task 14 live dogfood rerun (2026-09-23, fresh Docker-backed auto-bootstrap):** local bootstrap reached the authenticated Carnegie Home UI with PostgreSQL `55432`, Control Plane `4310`, and Model Gateway `4321`; the bounded window capture passed. A visible, documentation-only request failed truthfully at the provider/model boundary with `conversation not started`, `turn failed`, and `No Concertmaster model is available`. No durable IDs, provider response, Worker/evidence/certification/report records, or external effects exist. Fresh Electron/services/container were stopped after capture. Evidence: `/tmp/e5-live-rerun22-home-after-turn.png`, `/tmp/e5-live-rerun22-turn.log`, `execution/e5-live-evidence/README.md`.


- [ ] **plan-E5 Task 15 final gate — attempted 2026-09-23, not closed:** root build and Carnegie build passed; exact Carnegie Playwright passed 4 tests / skipped 5; diff check passed. Unrestricted `npm test` first failed at the unavailable `127.0.0.1:55432` endpoint after 336 tests passed. A disposable PGlite replacement was unsuitable for the full suite: repeated 30-second worker/evidence timeouts and an isolated `pg` `unexpected parseComplete` protocol error appeared; no code failure is claimed from that substitute. A real PostgreSQL-backed full suite is now green and live Control Plane/bootstrap acceptance reached the authenticated Home UI; configured provider/model availability remains required for a real assistant-to-Task Contract turn before E5 completion.


- [ ] **plan-E5 heartbeat #16 recheck (2026-09-23, `f282458c`):** root build, Carnegie build, focused E5/UI suite (8 files / 35 tests), exact Carnegie Playwright (4 passed / 5 designed CDP skips), diff check, and clean-tree check passed. Runtime remains unchanged: no Docker/native PostgreSQL, Control Plane, Model Gateway, or listener is available, so Task 14/15 cannot close and E6 remains gated.

- [ ] **plan-E5 Task 15 final verification recheck (2026-09-23):** real PostgreSQL full `npm test` completed with **432 test files / 2,874 tests passed, exit 0**; root and Carnegie builds passed; exact Carnegie Playwright passed **4** with **5 designed CDP-gated skips**; changed-file ESLint and `git diff --check` passed; and fresh secret/artifact scans were clean. Task 14 remains backend-blocked at existing embedded PGlite bootstrap (mutex timeout), with no Control Plane, Model Gateway, provider listener, or durable live IDs. E5 and E6 remain gated only by that live dependency, not by the automated suite. Evidence: `/tmp/e5-heartbeat19-full-real-pg.log`, `execution/e5-live-evidence/README.md`.
  Independent no-edit review passed the patch with no P0/P1 findings. Pre-existing P2 follow-up: the router integration test imports constants from another integration test, so focused Vitest collection registers that suite too; no E5 closure claim is based on the focused count.
- [ ] **plan-E5 Task 14 managed-provider retry (2026-09-23):** Docker-backed bootstrap and bounded Carnegie window capture passed. In-app ChatGPT/Codex binding completed; authenticated Model Gateway `/v1/models` returned five real Codex models. A visible documentation-only Home request created conversation `a352ec21-72fe-4e14-9f23-d35f54fa0f67` and completed with a real assistant response. The Home intake returned no Task Contract draft, so no contract ID/version/hash or Goal/Worker/evidence/certification/report records exist. Task 14 Steps 3–6 remain open; E5 is not closed and E6 remains gated. Evidence: `/tmp/e5-live-final2-after-turn.png`, `/tmp/e5-live-final2-terminal.png`, `/tmp/e5-live-final2-turn.log`, `/tmp/e5-live-final2-terminal.log`, `execution/e5-live-evidence/README.md`.

- [ ] **plan-E5 heartbeat #48/#49 model-selection slice (2026-09-24):** local `main` is 14 commits ahead of `origin/main`; the provider/model UX commits are `b4f9dce7`, `e331e1ea`, `9a80d468`, with documentation checkpoint `2d486263` and compatibility fix `b4d6e76a`. Focused reasoning/Gateway/Control Plane/Carnegie verification passed **11 files / 116 tests**; the compatibility regression set passed **4 files / 93 tests**; root and Carnegie builds, ESLint, boundary, migration, and `git diff --check` passed. A fresh unrestricted `npm test` against PostgreSQL `127.0.0.1:55432` completed **440 files / 2,916 tests, exit 0**, duration **1,001.90s**, log `/tmp/maestro-full-test-2.log`. No live provider/Task Contract/Goal/Worker evidence was produced by this slice, and E6 remains gated behind the integrated E5 acceptance.

- **Provider admission probe (2026-09-24):** started the native Model Gateway with `MAESTRO_OPERATOR_ID=local-operator` and a non-production probe token, queried authenticated `/v1/models`, and received `[]`. A gateway-owned metadata-only keychain listing for `local-operator` returned `[]`; no `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, or gateway credential environment variables are present. The probe process was stopped and no provider request was made. Live Overture role turns remain `provider_unavailable`; do not claim Task Contract, Goal, or Worker evidence.

- [ ] **Task 15 routing/bootstrap checkpoint (2026-09-24):** commit `ec3141a5` now fails closed when configured Ensemble candidates are missing from the live Gateway or use an unauthorized account binding, and forwards routing configuration through local bootstrap. Focused verification passed **3 files / 66 tests**; build and lint passed. The next implementation gap is the missing Overture clarification answer path above persistence; no live provider/Task Contract/Goal/Worker evidence exists.

- [ ] **Task 15 clarification-answer checkpoint (2026-09-24):** commit `11e11429` adds the scoped answer route, typed client, Electron bridge, safe clarification event payloads, and Home answer form. Focused verification passed **7 files / 75 tests**, including PostgreSQL Overture persistence (**6 tests**), with build/lint/diff checks green. The role-runtime resume/live provider gate remains open; no live provider/Task Contract/Goal/Worker evidence exists.

- [ ] **Heartbeat #51 full regression result (2026-09-24):** after the clarification-answer path and fail-closed event projection fix (`d6913d0e`), unrestricted `npm test` against PostgreSQL `127.0.0.1:55432` passed **440 files / 2,919 tests**, exit 0, duration **912.60s**. Log: `/tmp/maestro-full-test-3.log`. This is automated evidence only; live provider and downstream E5 evidence remain open.

- [ ] **Task 15 role-continuation regression (2026-09-24):** commit `4c02785a` is covered by a fresh unrestricted PostgreSQL-backed suite: **441 files / 2,920 tests**, exit 0, duration **916.12s** (`/tmp/maestro-full-test-4.log`). The live provider and downstream E5 evidence gate remains open.

## Task 16 exact Launch → Goal handoff checkpoint (2026-09-24)

The next narrow slice now couples exact Task Contract Launch to a server-derived Goal in one PostgreSQL transaction. The launch response returns `{ taskContract, goalId, scheduling: "queued" }`. The transaction writes the `GoalCreated` receipt/event, `goal_controls`, and `goal-events` outbox handoff, and marks a linked reviewed Overture Run launched. The nested Overture state event uses its own event-local command ID so the default Launch ID cannot collide with the earlier `task_contract_attached` event. Replay with the same or a new launch command reuses the unique Task Contract→Goal binding without creating a second Goal. Focused PostgreSQL/API/UI/CLI/Overture/surface verification passed **105 tests across 9 files**; `npm run build` passed. This is automated local evidence only: legacy contracts without a linked Overture Run remain supported, while the repository still has no outbox consumer that advances Head/Council/Worker orchestration and live provider access remains unavailable; E5 and E6 are not complete.


- [ ] **Task 16 first orchestration-command handoff (2026-09-24, automated only):** the GoalCreated `goal-events` outbox payload now carries a scoped `start_goal` command identity (`commandId` = event ID, plus project/Goal/Task Contract IDs), written atomically with Launch. RED failed before this metadata existed; GREEN passed `MAESTRO_TEST_DATABASE_URL=postgresql://maestro@127.0.0.1:55432/maestro_local npx vitest run apps/control-plane/src/task-contract-api.integration.test.ts --reporter=dot` (**1/1**), `MAESTRO_TEST_DATABASE_URL=postgresql://maestro@127.0.0.1:55432/maestro_local npx vitest run packages/persistence/src/commands.integration.test.ts --reporter=dot` (**16/16**), and `MAESTRO_TEST_DATABASE_URL=postgresql://maestro@127.0.0.1:55432/maestro_local npx vitest run packages/persistence/src/overture.integration.test.ts --reporter=dot` (**6/6**); build/typecheck/lint/diff checks passed. The consumer and automatic Head/Council/DepartmentPlan/MissionBundle/Worker progression remain missing, so E5 is not closed and no live downstream evidence is claimed.

- [ ] **Task 16 durable outbox lease prerequisite (2026-09-24, automated only):** PostgreSQL now provides `claimGoalOutbox` (`goal-events` topic, `SKIP LOCKED`, owner/expiry lease, attempts), owner-fenced `markGoalOutboxDelivered`, and owner-fenced `releaseGoalOutbox` for retry. Focused verification passed **19 tests across 2 files**, covering lease expiry/reclaim and retry. Build/typecheck/lint/diff checks passed. The typed `start_goal` consumer and automatic Head/Council/DepartmentPlan/MissionBundle/Worker progression are still missing; do not treat this as execution or live evidence.

- [ ] **Task 16 Launch-result Goal selection (2026-09-24, automated only):** after the server returns the exact Launch result, Carnegie refreshes the durable Goal list and selects the returned Goal only if PostgreSQL confirms it in that list. Focused verification passed **12 tests across 2 files**; full Carnegie verification passed **55 files / 249 tests**; build/typecheck/lint/diff checks passed. This is GUI state convergence only. Automatic downstream orchestration and live evidence remain open.

- [ ] **Task 16 post-Launch GUI regression (2026-09-24, automated only):** unrestricted `MAESTRO_TEST_DATABASE_URL=postgresql://maestro@127.0.0.1:55432/maestro_local npm test` passed **441 files / 2,926 tests**, exit 0, duration **927.57s**. Log: `/tmp/maestro-full-test-5.log`. Live provider and automatic downstream E5 evidence remain open.

- [ ] **Task 16 typed `start_goal` envelope guard (2026-09-24, automated only):** claimed `goal-events` rows can now be parsed only when event identity, command identity, exact type, and UUID scope agree; malformed input fails with a fixed redacted error. Focused PostgreSQL/persistence verification passed **30 tests across 3 files**; build/typecheck/lint/diff checks passed. This parser does not consume, ack, transition, or execute anything. The typed consumer and downstream progression remain missing.

- [ ] **Task 16 post-parser regression (2026-09-24, automated only):** unrestricted `MAESTRO_TEST_DATABASE_URL=postgresql://maestro@127.0.0.1:55432/maestro_local npm test` passed **441 files / 2,931 tests**, exit 0, duration **904.09s**. Log: `/tmp/maestro-full-test-6.log`. Live provider and automatic downstream E5 evidence remain open.

- [ ] **Task 16 durable `start_goal` binding (2026-09-24, automated only):** a claimed envelope is now checked against PostgreSQL `goal_events`, `goals`, and launched `task_contracts`, including canonical content hash and project scope. Focused verification passed **30 tests across 3 files**; build/typecheck/lint/diff checks passed. This validator does not ack or execute the command. Consumer retry/quarantine and downstream progression remain missing.

- [ ] **Task 16 regression follow-up (2026-09-24):** post-binding unrestricted PostgreSQL `npm test` reached 441 files and 2,930/2,931 tests before one soak lock race failed (`/tmp/maestro-full-test-7.log`, exit 1, 918.47s). The race was fixed with atomic lock publication in `scripts/run-soak.mjs`; focused 5/5 plus four parallel repeats, build/typecheck/lint/diff passed. Fresh full regression pending.

- [ ] **Task 16 post-soak-fix regression (2026-09-24, automated only):** after `5634e9d1`, unrestricted PostgreSQL `npm test` passed **441 files / 2,931 tests**, exit 0 (`/tmp/maestro-full-test-8.log`). The atomic soak-lock fix and binding validator are covered; live provider and automatic downstream execution evidence remain open.

- [ ] **Task 16 typed start_goal consumer (2026-09-24, automated only):** explicit `MAESTRO_START_GOAL_OUTBOX_INTERVAL_MS` now starts a restart-safe loop after startup reconciliation. It claims only typed `start_goal` rows, validates durable binding, fences delivery, retries transient validation failures, and leaves generic Goal events untouched. Focused verification passed **46 tests across 4 files**; main wiring passed **10 tests**; build/typecheck/lint/diff passed. No downstream entities or provider effects are created; automatic progression remains missing.

- [ ] **Task 16 Launch error mapping (2026-09-24, automated only):** unavailable Goal orchestration now returns stable HTTP 503 `task_contract_orchestration_unavailable` instead of generic durable-store error. API/contracts verification passed **20 tests across 2 files**; typecheck/build passed. This does not make orchestration available or provide live evidence.

- [ ] **Task 16 pre-build regression note (2026-09-24):** full run 9 exited 1 after 2,940/2,941 tests because it started before the new API error schema was built and loaded stale contracts dist; only the new API mapping test failed (`/tmp/maestro-full-test-9.log`, 980.36s). Post-build API/contracts focused verification passed 20/20; fresh full regression pending.

- [ ] **Task 16 API error title follow-up (2026-09-24):** full run 10 found one missing Carnegie title for `task_contract_orchestration_unavailable` after 2,940/2,941 tests (`/tmp/maestro-full-test-10.log`, 901.31s). Stable title added; focused command-id/API tests passed 16/16 and build/typecheck/lint/diff passed. Fresh full regression pending.

- [ ] **Task 16 full regression run 11 timing note (2026-09-24):** unrestricted PostgreSQL `npm test` exited 1 after **441 files / 2,941 of 2,942 tests**, 898.53s (`/tmp/maestro-full-test-11.log`). The only failure was the worker owner-claim cancellation test with an intentionally 250ms lease; `cancelWorker` correctly failed closed with `StaleGoalLeaseError` before its second claim under full-suite DB load, while takeover fulfilled. Five isolated repetitions and the full worker integration file passed (46/46). No production change made; fresh full regression pending.


- [ ] **Task 16 full regression run 12 (2026-09-24):** unrestricted PostgreSQL `npm test` passed **442 test files / 2,942 tests**, exit 0, duration **963.28s** (`/tmp/maestro-full-test-12.log`). This closes the prior automated regression requirement after run 11's timing-sensitive failure; live provider, downstream orchestration, and E5 acceptance remain open.

- [ ] **Task 16 explicit Head activation plan/controller slice (2026-09-24):** code-level focused tests cover hash-bound explicit department briefs, durable `running|blocked|unknown|completed` Head-stage state/history, draft-to-active lifecycle, deterministic per-department command IDs, and execute-before-ack retry behavior. Missing plans fail closed as durable `blocked`; non-active Head outcomes fail closed as durable `unknown`. No live provider or downstream evidence exists.


- [x] **Task 16 run 13 investigation (2026-09-24):** non-green because the run started before the final hardening and surface snapshot/persistence-fixture updates: **445 files / 2,952 tests**, 5 failures, exit 1, **1034.66s** (`/tmp/maestro-full-test-13.log`).

- [ ] **Task 16 fresh run 14 (2026-09-24):** unrestricted PostgreSQL `npm test` passed **446 files / 2,954 tests**, exit 0, **949.77s** (`/tmp/maestro-full-test-14.log`). Automated gates are green; live provider, downstream stage, certification/report, and E5 acceptance evidence remain pending.
