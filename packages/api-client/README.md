# API Client

Typed client for authenticated Control Plane HTTP routes, including durable reads, commands, event streams, and native conversation metadata. It does not contact providers directly.

```sh
npm test -- packages/api-client/src
```

