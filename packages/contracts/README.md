# Contracts

Strict API request/response and event schemas shared by Control Plane clients and services. Contracts reject unknown fields and preserve provider-qualified model identity without carrying credentials.

```sh
npm test -- packages/contracts/src
```

