# Domain

Pure domain types, invariants, hashes, state machines, and execution contracts. This package has no PostgreSQL, network, provider, or filesystem authority.

Important contracts include Mission Bundles, Goal/lease state, capability grants, `ExecutionKernelPort`, certification, evidence, devices, and orchestration records.

```sh
npm test -- packages/domain/src
```

