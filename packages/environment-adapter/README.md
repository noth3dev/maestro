# Environment Adapter

Capability-scoped adapters for browser and local environment execution. They consume host grants and return normalized evidence; they do not authorize actions themselves.

```sh
npm test -- packages/environment-adapter/src
```

