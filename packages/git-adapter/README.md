# Git Adapter

Narrow local Git port used behind Maestro authority checks. It records repository, branch, worktree, commit, and integration effects; callers must provide the Control Plane action context.

```sh
npm test -- packages/git-adapter/src
```

